var backbone, WorkerCollection, monologue, base;

monologue = require('monologue');

backbone = require('backbone');



WorkerCollection = function(name, properties) {
	var protoModel, Collection;
	
};

module.exports = WorkerCollection;